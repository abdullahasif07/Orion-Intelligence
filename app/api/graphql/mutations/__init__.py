"""Mutation resolvers.

Empty for the MVP — the `ask` operation is read-only (no state persisted).
Add modules here once we introduce features like chat history, saved
briefings, or user-managed tool config. The pattern is identical to
``app.api.graphql.queries``: one resolver per file, re-exported here.
"""

__all__: list[str] = []
